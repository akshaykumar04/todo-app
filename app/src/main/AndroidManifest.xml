<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.sstechcanada.todo">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".activities.TodoListActivity"
                android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name="com.sstechcanada.todo.activities.SettingsActivity"
                android:parentActivityName="com.sstechcanada.todo.activities.TodoListActivity">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.sstechcanada.todo.activities.TodoListActivity"/>
        </activity>

        <activity
                android:name="com.sstechcanada.todo.activities.AddOrEditTaskActivity"
                android:parentActivityName="com.sstechcanada.todo.activities.TodoListActivity">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.sstechcanada.todo.activities.TodoListActivity"/>
        </activity>

        <provider
                android:name="com.sstechcanada.todo.data.TodoListProvider"
                android:authorities="com.sstechcanada.todo"
                android:exported="true"/>

        <receiver android:name="com.sstechcanada.todo.widget.TodoListWidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>

            <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/todo_list_widget_info"/>
        </receiver>

        <service
                android:name="com.sstechcanada.todo.widget.TodoListWidgetRemoteViewsService"
                android:permission="android.permission.BIND_REMOTEVIEWS">
        </service>

        <receiver
                android:name="com.sstechcanada.todo.broadcast_receivers.DailyAlarmReceiver"
                android:process=":remote">
        </receiver>

        <service
                android:name="com.sstechcanada.todo.services.DueCheckIntentService"
                android:exported="false"/>

    </application>

</manifest>